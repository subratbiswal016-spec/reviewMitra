import { defineConfig } from 'vite'; import react from '@vitejs/plugin-react'; export default defineConfig({ plugins: [react()], build: { emptyOutDir: false, rollupOptions: { input: 'src/content.tsx', output: { entryFileNames: 'content.js', format: 'iife', name: 'content' } } } });
